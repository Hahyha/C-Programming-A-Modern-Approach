There are two reasons why sizeof(a) / sizeof(a[0]) is better than sizeof(a) / sizeof(t).
First of all, it's more readable and understandable.
Secondly, when we change the data type of a or t singly, the latter might result in errors.
